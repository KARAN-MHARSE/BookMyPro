package com.bookmypro.identity_service.feature.auth.createcredential;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;

@RestController
@RequestMapping("/auth")
@RequiredArgsConstructor
public class CreateCredentialController {

	
	private final CreateCredentialService createCredentialService;

    @PostMapping("/credential")
    public ResponseEntity<CreateCredentialResponse> register(
            @Valid @RequestBody CreateCredentialRequest request) {

        CreateCredentialResponse response =
                createCredentialService.createCredential(request);

        return ResponseEntity
                .status(HttpStatus.CREATED)
                .body(response);
    }
}
